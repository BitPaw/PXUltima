#pragma once

#include <Media/IP/PXIP.h>

namespace PX
{  
    class IP
    {        
        //PXIPVersion PXIPAnalyseVersion(const char* ip);
        //static PXByte PXIPIsValid(const char* ipAdress);
        //static bool PXIPv4CheckIfValid(const char* ipv4Adress);
        //static bool PXIPv6CheckIfValid(const char* ipv6Adress);
    };
}